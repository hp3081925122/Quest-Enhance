# 更新日志

## 1.20.1-forge-3.6.2 (2026-08-19)

### 优化
- 任务描述编辑器「按键绑定」组件新增**多选**能力：打开选择列表后每条按键前显示勾选框（`[x]` / `[ ]`），点击仅切换勾选状态并实时刷新列表；底部「确认」按钮（或 Shift+Enter）将**所有勾选的按键分别插入为一行**（每条独立成行，仍为「动作名：按键」格式），不再需要一个一个单选。
- 编辑已有按键绑定时仍保持单选即时确认，避免一次替换成多个。

## 1.20.1-forge-3.6.1 (2026-08-19)

### 优化
- 任务描述编辑器「按键绑定」组件：由手动输入技术键改为**带搜索框的下拉选择列表**（复用 AbstractButtonListScreen 的搜索框），列出当前游戏所有已注册的按键绑定，显示翻译名并附技术键（如「跳跃 (key.jump)」），可按名称或技术键实时筛选，不再需要记忆或手敲技术键。
- 选中的按键绑定插入描述时，同时显示**动作翻译名 + 按键图标**（如「跳跃 [空格]」），不再孤零零只显示一个按键图标，便于玩家识别该按键对应什么操作。

## 1.20.1-forge-3.6

### 初始版本
- 剪贴板图片粘贴、章节画布文字、任务实体模型显示、视频/GIF 播放、任务描述增强组件、KubeJS 集成等编辑增强功能。
